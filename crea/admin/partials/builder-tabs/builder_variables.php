<?php
/**
 * Ruta del archivo: wp-content/plugins/crea/admin/partials/builder-tabs/builder_variables.php
 *
 * Contenido de la pestaña "Variables (Columnas)".
 * Gestiona la asignación de columnas, tipos de datos y reglas a una base de datos existente.
 */

if ( ! defined( 'WPINC' ) ) {
	die;
}
?>

<div class="crea-card" style="border-top: 4px solid var(--crea-primary);">
	<div class="crea-card-header">
		<h2>Gestor de Variables y Columnas</h2>
	</div>
	<p>En esta pestaña seleccionaremos una base de datos creada y le añadiremos las variables (texto, número, fecha, etc.).</p>
</div>